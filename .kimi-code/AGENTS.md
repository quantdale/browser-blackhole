# Planner/Executor Handoff Adapter

Preserve and obey root/scoped `AGENTS.md` and repository governance/state files. For `/goal continue`: read `.agent/PLANNER_HANDOFF.md`, `.agent/EXECUTION_PROMPT.md` if present, and native campaign/state/OpenSpec files; reconcile with current Git and implementation; if the prompt is `ACTIVE`, resume the first genuinely incomplete requirement and continue autonomously through required validation, durable-state updates, commits, and pushes. If absent/completed, fall back to native continuation semantics. If neither exists, do not invent a major campaign; report that the planner must create the next execution prompt. Never redo landed work, weaken stricter local rules, perform unrelated rewrites, or finish with known Critical/High regressions.